// swift-tools-version: 6.2

import PackageDescription

// Declare the LumemoKit package — pure Swift, cross-platform, no UI.
//
// What it needs:
//   - a name
//   - a list of platforms with minimum versions for each Apple platform you support
//   - a list of products — at least one library product exposing LumemoKit
//   - a list of targets — the LumemoKit target itself plus a test target
//     that depends on it
