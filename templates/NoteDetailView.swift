import SwiftUI

// Declare the screen that shows a single note's body.
//
// What it needs:
//   - public visibility
//   - a stored note property and a public initializer accepting it
//   - a body that displays the note's body text in a ScrollView, with
//     the navigation title set to the note's title
