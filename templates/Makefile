.PHONY: install-dependencies

## install-dependencies
## What does this target need to do? Install everything pinned in .mise.toml
## so the rest of the project's tools are available locally and in CI.
install-dependencies:
