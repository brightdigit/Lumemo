import SwiftUI

// Declare the root view shown by the app.
//
// What it needs:
//   - conformance to the View protocol
//   - a body returning some View — for now, a single text label naming
//     the app is enough. The real UI arrives in Phase 3.
